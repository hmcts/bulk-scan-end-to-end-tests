#!groovy

properties([
  pipelineTriggers([cron('*/30 * * * *')])
])

@Library("Infrastructure")

def type = "java"
def product = "hmcts"
def component = "bulk-scan-end-to-end-tests"

def channel = '#bsp-build-notices'

withPipeline(type, product, component) {
    nonServiceApp()

    enableSlackNotifications(channel)
}
